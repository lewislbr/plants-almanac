{
  "name": "plants-almanac",
  "description": "A plants information directory.",
  "author": "@lewislbr",
  "repository": "https://github.com/lewislbr/plants-almanac.git",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently --kill-others-on-fail \"cd data && npm run start:dev\" \"cd ui && npm run start\"",
    "lint": "concurrently \"cd data && npm run lint\" \"cd ui && npm run lint\"",
    "format": "concurrently \"cd data && npm run format\" \"cd ui && npm run format\""
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.1.0",
    "husky": "^4.2.5",
    "lint-staged": "^10.1.7"
  }
}
