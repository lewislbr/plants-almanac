{
  "name": "@plants-almanac/server",
  "version": "1.0.0",
  "main": "dist/app.js",
  "author": "Lewis Llobera <billobera@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./dist",
    "check": "tsc",
    "start": "tsc && rm -rf ./dist && webpack --mode=development --watch && node dist/index.js",
    "build": "tsc && rm -rf ./dist && webpack --mode=production"
  },
  "dependencies": {
    "apollo-server": "^2.9.2",
    "graphql": "^14.5.0",
    "mongoose": "^5.6.10"
  },
  "devDependencies": {
    "@babel/plugin-proposal-export-default-from": "^7.5.2",
    "@types/graphql": "^14.2.3",
    "@types/mongoose": "^5.5.13",
    "nodemon-webpack-plugin": "^4.0.8"
  }
}
