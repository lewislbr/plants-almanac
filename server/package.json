{
  "name": "@plants-almanac/server",
  "version": "1.0.0",
  "main": "dist/app.js",
  "author": "Lewis Llobera <billobera@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./dist",
    "check": "tsc",
    "start": "tsc && rm -rf ./dist && webpack --mode=development --watch && node dist/app.js",
    "build": "tsc && rm -rf ./dist && webpack --mode=production"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-graphql": "^0.9.0",
    "graphql": "^14.5.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/graphql": "^14.2.3",
    "nodemon-webpack-plugin": "^4.0.8"
  }
}
